# Rust build output
/target/

# Backtrace / debug artifacts
*.pdb

# Packaging output (built by packaging/build-appimage.sh and cargo deb/wix,
# published as release assets rather than committed)
*.AppImage
*.deb
*.msi
/squashfs-root/

# Cargo — keep Cargo.lock for the binary crate (reproducible builds),
# so it is intentionally NOT ignored.

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# OS junk
.DS_Store
Thumbs.db

_bmad
.claude

# Local planning docs — kept in-repo for convenience, never published
/plans/
