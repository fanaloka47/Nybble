[package]
name = "nybble-core"
version = "1.1.0"
edition = "2021"
description = "Pure numeric core for Nybble: multi-base values, width/signedness, ops, expressions."

[lib]
name = "nybble_core"
path = "src/lib.rs"
